<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:orientation="horizontal" android:gravity="end"
    android:paddingStart="12dp" android:paddingEnd="12dp" android:paddingTop="6dp" android:paddingBottom="6dp">

    <LinearLayout
        android:layout_width="0dp" android:layout_height="wrap_content"
        android:layout_weight="1" android:gravity="end">

        <LinearLayout
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:background="@drawable/bg_bubble_user"
            android:padding="10dp">

            <TextView
                android:id="@+id/tvUserText"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:textColor="@color/userTextOn"
                android:textSize="14sp"
                android:lineSpacingExtra="2dp"/>
        </LinearLayout>
    </LinearLayout>

    <!-- avatar user (placeholder) -->
    <TextView
        android:layout_width="28dp" android:layout_height="28dp"
        android:text="Bạn" android:textSize="11sp"
        android:gravity="center"
        android:background="@drawable/bg_bubble_bot"
        android:padding="0dp" android:textColor="#374151"
        android:layout_marginStart="6dp"/>
</LinearLayout>
